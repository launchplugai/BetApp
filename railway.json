{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["app/**", "analytics/**", "context/**", "docs/**", "requirements.txt", "railway.json", "dna-matrix/**"],
    "buildCommand": "pip install -e ./dna-matrix"
  },
  "deploy": {
    "startCommand": "sh -c 'PYTHONPATH=/app:/app/dna-matrix:$PYTHONPATH alembic upgrade head && PYTHONPATH=/app:/app/dna-matrix:$PYTHONPATH uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
