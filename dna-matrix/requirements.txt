# Core
# (no external dependencies for core models yet)

# Testing
pytest>=8.0.0,<10.0.0

# API
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.7  # CVE-2024-24762 fix

# Development
black>=24.0.0
mypy>=1.8.0
ruff>=0.5.0
