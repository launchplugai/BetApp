[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dna-matrix-core"
version = "0.1.0"
description = "DNA Matrix core evaluation engine"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["core*"]
